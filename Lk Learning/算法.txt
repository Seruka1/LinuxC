贪心、双指针（滑动窗口）、二分（最大值的最小值/最小值的最大值 二分答案）、排序（左端点/右端点排序，置换环）、搜索（回溯、记忆化搜索、 bfs（最短路，多源bfs，记录步数的技巧）、dfs）、
动规（0-1背包、状压dp、区间dp、数位dp、线性dp、树形dp、矩阵快速幂）、分治与递归、数学（gcd贝祖定理、埃氏筛欧拉筛）、位、枚举
stl（数组、栈、队列、单调栈、优先队列（On建堆 完全二叉树+自底向上更新）、双端队列、哈希表（分桶）、前缀和（差分）积分图、多重映射、字典树）
字符串（KMP还没学）
链表（反转，合并，归并排序）
树（递归，层次遍历，前中后序遍历（递归、迭代、morris），二分查找树，字典树（0-1Trie用于解决异或），倍增lca求最近公共祖先/第k个祖先，定长区间和)
图（二分图（并查集，广度优先、深度优先），拓扑排序（处理的元素有先后顺序），最短路径（权值出现0与1的情况考虑0-1bfs   (0-1)dijkstra和弗洛伊德  记录路径的方法（每次松弛记录点的前一个点）
，bellman ford），最小生成树（prim和kruskal）、欧拉回路（K出度与k入度 那么可以一笔走遍）、环（bfs/dfs/并查集）
复杂数据结构（并查集）

区间（单点）处理（区间最大值，区间值总和）    树状数组、线段树、差分数组（一维、二维  最大区间重叠数，上下车问题）
双堆处理会议室问题

矩阵快速幂  （多米诺、动规 齐次线性递推式转为求矩阵的幂）、上取整技巧

高阶： 折半搜索、状态压缩、
问题转换 如统计m=k=(m>=k)-(m>=k+1) 正难则反  求low<=k<=high 转为求k<=x          自变量太多？考虑值域（1819）
离线询问思想

根据数据量  不要忘了枚举


哈夫曼树  每次把权值最小的两棵二叉树合并（利用最小堆实现）
（带权路径长度最小的二叉树，可以用于编码（根据频率出现的次数来编码））